# Two-Currency Engine — PLANNED

**Status:** planned (rebuild on the harness, K=2)
**Question:** With energy (open) + nutrient (closed), does the emergent E/N **quality axis** fall out
and decline along the detrital path (food → fertilizer)?

## Scenario (to build)
K=2 (energy open, nutrient closed): `mineral → grass → detritus → fungus → mineral`. Two conservation
gates (energy `Solar = stored + Heat`; nutrient total invariant); the emergent E/N gradient
(grass high → detritus → fungus → mineral 0).

## Run
*(to be written — copy `../_template/`)* → `outputs/`.

## Notes
Rebuild of `scripts/two_currency.jl` as a harness `Scenario` (the engine already supports it).
See `docs/journal.md` (2026-06-08, two-currency engine).
